# Tutorials

[![GitHub](https://img.shields.io/badge/GitHub-181717?style=flat&logo=github&logoColor=white)](https://github.com/kurtvalcorza/whisper-asr-pipeline)
[![Open In Colab](https://colab.research.google.com/assets/colab-badge.svg)](https://colab.research.google.com/github/kurtvalcorza/whisper-asr-pipeline/blob/main/tutorials/whisper_asr_colab.ipynb)
[![Hugging Face](https://img.shields.io/badge/%F0%9F%A4%97%20Hugging%20Face-openai%2Fwhisper--large--v3--turbo-ffcc4d?style=flat)](https://huggingface.co/openai/whisper-large-v3-turbo)
[![Upstream](https://img.shields.io/badge/Upstream-openai%2Fwhisper-181717?style=flat&logo=github&logoColor=white)](https://github.com/openai/whisper)
[![arXiv](https://img.shields.io/badge/arXiv-2212.04356-b31b1b.svg)](https://arxiv.org/abs/2212.04356)

Notebook specification: **DIMER Notebook Specification 2.0** — the notebook is **standalone** (§4) and declares its profile and pedagogical mode (§3.4): it is
generated by `tools/build_notebook.py` from `tools/notebook_template.py` and carries the repository's
pipeline module, model identity, snapshot manifest and runtime pins, so the exported `.ipynb` keeps working
without this repository being reachable. Do not edit the notebook by hand; edit the package or the template
and regenerate (`python tools/build_notebook.py`; `--check` is enforced by the validator and CI).

| Notebook | Profile | Mode | Carrier | Capability | Default runtime | Sample | BYOD | Run-all | Release status |
|---|---|---|---|---|---|---|---|---|---|
| `whisper_asr_colab.ipynb` | `TASK-INFERENCE` | `GUIDED` | standalone (generated) | Multilingual automatic speech recognition with `openai/whisper-large-v3-turbo` (transcribe; translate-to-English available); `validate_inputs` → input manifest; `evaluation_report` → `sample-sanity` with `word_error_rate` on the public referenced utterance (or a BYOD `REFERENCE_TEXT`), `not-measurable` without a reference | CPU (CUDA used automatically when available) | automatic | single audio file, gated off by default; optional reference transcript | unverified — no clean-runtime `Run all` execution recorded for this revision (REL1/REL8) | **Candidate** — static checks pass; the clean-runtime execution run of the standalone notebook is pending and will be recorded in `../docs/release-verification.md`, which must be reviewed for the exact notebook revision before promotion |
| `whisper_asr_finetune_colab.ipynb` | `E2E` | `GUIDED` | standalone (generated from `tools/notebook_template_finetune.py`, pins from `tools/finetune-pins.txt`) | LoRA adaptation of the pinned `openai/whisper-large-v3-turbo` weights (q/v projections, rank 32) on one locale of `PolyAI/minds14` at a pinned dataset revision: every clip validated through `validate_inputs` into one input manifest (30 s ceiling enforced, never truncated), zero-shot baseline corpus WER through the carried pipeline, plain-PyTorch AdamW loop under mixed precision with the loss history as optimisation evidence only, adapted corpus WER through the same pipeline, `adaptation_report` → `sample-sanity`, `export_adapter_bundle` (safetensors adapter + metrics + provenance + manifest, zipped), fresh reload from the verified base snapshot with `verify_adapter_merge` (weight-level `W_base + (alpha/r)·B@A` check) and ≥95% transcript agreement | 16 GiB-class CUDA GPU (Colab T4 / Kaggle P100–T4); CPU only as a reduced smoke test | automatic (`PolyAI/minds14` `en-US`, seeded 400/100 split) | `transcripts.csv` + audio files, gated `USE_BYOD` upload (off by default) | unverified — no clean-runtime `Run all` execution recorded for this standalone revision (REL1/REL8); the Kaggle P100 run in `../docs/release-verification.md` covers the previous clone-based revision `db4daf8` only | **Candidate** — static checks pass; a clean GPU-runtime execution of this exact revision must be recorded before promotion |

## Conformance notes

- **Fine-tuning notebook (E2E, migrated to the standalone carrier 2026-09-14):** the previous clone-based Version-1 notebook was replaced by generator output. The adaptation, evaluation, export and reload stages call the carried module's `load_model(weights_dir=…)`, `corpus_word_error_rate`, `export_adapter_bundle`, `verify_adapter_merge` and `adaptation_report`; `WhisperASRPipeline.from_pretrained(weights_dir=WEIGHTS_DIR, adapter_dir=…)` reloads against the digest-verified snapshot staged in Section 3, so no second download and no Hub call happens after Section 3. `tests/test_finetune_parity.py` holds PAR1–PAR3 for this notebook and pins `tools/finetune-pins.txt` to `pyproject.toml` plus the `tutorial`/`finetune` extras (ENV2). Known 2.0 caveats: the default path is GPU-sized (RUN11 stated up front; CPU runs but takes hours) and no dispersion estimate accompanies the WER figures (EVAL5 — one seeded split).
_Requirement identifiers in these notes (for example `ST1`, `PAR3`, `DAT24`, `EVAL21`) are the NOTEBOOK_SPEC 1.1 numbering they were written against; NOTEBOOK_SPEC 2.0 renumbered its sections and the pull request that migrated this repository reports conformance against the 2.0 pre-flight checklist (§26) and report format (§30)._

- **Standalone carrier (ST1–ST6), both notebooks:** the primary path performs no clone, repository install, or repository import. Section 2 is `src/whisper_asr_pipeline/pipeline.py` verbatim (tagged `metadata.dimer.embedded_module`; the only rewrite makes the default weights directory working-directory-relative), Section 3 carries the model identity and the 12-file SHA-256 manifest inline and asserts they agree with the module before anything is fetched, and Section 1 carries the exact `pyproject.toml` runtime pins. `metadata.dimer.generated_from` records the repository, the generating revision, the module's SHA-256, and the generator version.
- **Parity (PAR1–PAR4):** `tests/test_notebook_parity.py` and `tools/validate_release_assets.py` fail when the carried module, the inline manifest, or the inline pins differ from the repository, or when the notebook is not byte-identical to the generator's output.
- **Model acquisition:** `stage_missing_files(WEIGHTS_DIR, allow_download=True)` fetches only the manifest entries that are absent (on a fresh runtime all twelve: configs, tokenizer files, `README.md`, the 1.6 GB `model.safetensors`), from the Hugging Face Hub at the immutable revision (never `main`); `verify_snapshot` re-hashes every entry; `from_pretrained(weights_dir=WEIGHTS_DIR)` hands the verified directory to `AutoProcessor` / `AutoModelForSpeechSeq2Seq` with `trust_remote_code=False` (the Whisper implementation is the pinned `transformers==4.52.1` one; no model-repository code runs).
- **Role stages:** `validate_inputs` (DAT24) writes `outputs/whisper_asr_input_manifest.json` with the schema, ceilings (`TASKS`, `MIN_CHUNK_LENGTH_S`, `MAX_CHUNK_LENGTH_S`), the observed input, the request, the verdict, and one recorded rejection finding (an oversized chunk length); `evaluation_report` (EVAL21) writes `outputs/whisper_asr_evaluation_report.json` — `sample-sanity` with `word_error_rate` when a reference transcript exists, `not-measurable` (with what labelled audio would make the task measurable) otherwise.
- The default sample is the first `validation` utterance of the public `hf-internal-testing/librispeech_asr_dummy` dataset, loaded through the pinned `datasets==4.4.0` at dataset revision `5be91486e11a2d616f4ec5db8d3fd248585ac07a` and decoded with the pinned `soundfile`; its WER is sanity evidence for one utterance, not a benchmark claim. `datasets` was promoted from the `tutorial` extra to a runtime pin so the standalone install cell carries it (ENV2/PAR2).
- WER normalisation is stated in the notebook and the card: case-folded, punctuation removed, curly apostrophes folded; abbreviations and numbers are not normalised.
- `USE_BYOD` defaults to `False` so the sample path never opens an upload dialog; `REFERENCE_TEXT` defaults to empty (no reference → `not-measurable`).
- `tools/validate_release_assets.py` performs source validation only. It does not satisfy the
  clean-runtime execution requirement; a release review must confirm that a recorded clean run in
  `docs/release-verification.md` matches the notebook revision under review before the status is
  promoted to `Release-grade`.
